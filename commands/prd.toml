description = "Use when writing a PRD, product spec, or product requirements — follows Google/Amazon/Stripe methodologies"
argument-hint = "<feature name>"
prompt = "Activating the spec-forge skill: prd-generation. Please process the following input or context: {{args}}. If no input is provided, proceed with the default workflow for prd-generation."
