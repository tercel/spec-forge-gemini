description = "Use when a project may need splitting into sub-features — analyzes scope and generates feature manifest with dependencies"
argument-hint = "<project name>"
prompt = "Activating the spec-forge:decompose. Please process the following input or context: {{args}}. If no input is provided, proceed with the default workflow for decompose."
